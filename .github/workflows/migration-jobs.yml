name: "Migrate jobs"
on:
  workflow_dispatch:
  
  schedule:
    - cron:  '0 4-18/1 * * *'

jobs:
  migrate_cases:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        court: [courts, appellate]
        server: [develop]
    name: ${{ matrix.court }}.${{ matrix.server }}
    
    steps:
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '7.4'
      - uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.PANTHEON_SSH_KEY }}
          config: ${{ secrets.SSH_CONFIG }}
          known_hosts: ${{ secrets.KNOWN_HOSTS }}
      - name: Install Terminus
        uses: pantheon-systems/terminus-github-actions@main
        with:
          pantheon-machine-token: ${{ secrets.PANTHEON_MACHINE_TOKEN }}
      - name: Stop running migrations
        continue-on-error: true
        run: |
          terminus drush jcc-courts.${{ matrix.server }} -- migrate:stop courts_node_job
          terminus drush jcc-appellate.${{ matrix.server }} -- migrate:stop appellate_node_job
      - name: Reset migrations
        continue-on-error: true
        run: |
          terminus drush jcc-courts.${{ matrix.server }} -- migrate:reset courts_node_job
          terminus drush jcc-appellate.${{ matrix.server }} -- migrate:reset appellate_node_job
      - name: Migrate All Jobs
        continue-on-error: true
        run: terminus drush jcc-courts.${{ matrix.server }} -- migrate:import courts_node_job
      - name: Migrate Appellate Jobs
        continue-on-error: true
        run: terminus drush jcc-appellate.${{ matrix.server }} -- migrate:import appellate_node_job
      - name: Clear cache
        continue-on-error: true
        run: terminus drush jcc-${{ matrix.court }}.${{ matrix.server }} -- cr
